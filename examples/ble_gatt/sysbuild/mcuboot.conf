# Enable QSPI driver in MCUboot
CONFIG_NORDIC_QSPI_NOR=y

# Increase max image sectors for 944KB partitions (944 / 4 = 236 sectors)
CONFIG_BOOT_MAX_IMG_SECTORS=256

CONFIG_USB_DEVICE_STACK=n
CONFIG_CONSOLE=y
CONFIG_UART_CONSOLE=y
CONFIG_LOG=y
