# MCUboot standalone build configuration for XIAO nRF54L15

# Flash configuration
CONFIG_FLASH_LOAD_OFFSET=0x0

# Boot configuration  
CONFIG_BOOT_MAX_IMG_SECTORS=256

# Disable USB (MCUboot doesn't need it)
CONFIG_USB_DEVICE_STACK=n
CONFIG_UART_CONSOLE=y
